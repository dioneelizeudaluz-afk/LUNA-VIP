# LUNA VIP

Painel administrativo para bot Telegram.

## Configuração

1. Crie um arquivo `.env` com as variáveis do `.env.example`
2. Configure o Supabase
3. Configure o Telegram Bot Token
4. Execute `npm install` e `npm run dev`
